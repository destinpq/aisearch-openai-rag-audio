# Frontend local env for Next.js
# Port used by Next dev/start (kept in sync with package.json)
PORT=2357
# Backend origin used by frontend code if referenced via process.env
NEXT_PUBLIC_BACKEND_ORIGIN=http://localhost:2355
